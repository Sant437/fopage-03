// src/types.ts
export interface Chat {
    name: string;
    message: string;
    time: string;
    avatar: string;
    status: string;
    mobileNumber: string; // Make sure this matches across your app
  }
  